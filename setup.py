from setuptools import setup, find_packages

setup(
    name="voice-timelogger-agent",
    version="1.0.0",
    description="AI-powered system that automates consultant work hours tracking using voice messages",
    author="Mohammad Othman",
    packages=find_packages(),
    python_requires=">=3.7",
)